import CardList from "../components/CardList";

function Browse() {
  // const [count, setCount] = useState(0);

  return (
    <div className="App">
      <CardList />
    </div>
  );
}

export default Browse;
