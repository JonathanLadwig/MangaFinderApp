import NavBar from "../components/Navbar";

function Library() {
  // const [count, setCount] = useState(0);

  return (
    <div className="App">
      <NavBar />
    </div>
  );
}

export default Library;
